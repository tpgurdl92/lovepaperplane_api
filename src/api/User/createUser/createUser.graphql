type Query {
  empty: String
}

type Mutation {
  createUser(
    username: String!
    birthDate: String!
    gender: String!
    location: String!
    nickname: String!
    machineId: String!
  ): MaintenanceData
}
